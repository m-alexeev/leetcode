def compressString(s: str) -> int:
    l = 0
    r = 1
    cur_char_count = 0
    cur_char = s[0]
    N = len(s)
    while r < N:
        if s[r] != cur_char:
            if cur_char_count > 1:

    return l


a = ["a","a","b","b","c","c","c"]

